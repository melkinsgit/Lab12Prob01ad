package com.margaret;

import java.sql.PreparedStatement;

/**
 * Created by sn0173nd on 12/1/2015.
 */
public class Junk {
//    String prepStatInsert = "INSERT INTO Dogs VALUES ( ? , ? )";
//    PreparedStatement psInsert = conn.prepareStatement(prepStatInsert);
//    psInsert.setString(1, "Tiny"); //Start counting from 1…
//    psInsert.setInt(2, 4); //now this is should be an int
//    psInsert.executeUpdate();
////Reuse the same preparedStatement to enter more data
//    psInsert.setString(1, "Einstein");
//    psInsert.setInt(2, 5);
//    psInsert.executeUpdate();
//    psInsert.close(); //Should close prepared statement, this would go in one of those try-catch blocks at the end of the program

}
